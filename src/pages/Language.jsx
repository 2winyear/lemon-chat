import { useNavigate } from "react-router-dom";

const navigate = useNavigate();

const Language = () => {
    navigate("/language");

    return (
        <div>
            hi
        </div>
    )
};